swagger: '2.0'
info:
  title: Json Rpc Infrastructure API
  description: rpc server {{.ServerName}}
  contact:
    name: {{.Contact}}
    email: {{.Email}}
  version: 1.3.0
consumes:
  - application/json
produces:
  - application/json

{{ template "path" . }}


{{ template "definition" . }}















